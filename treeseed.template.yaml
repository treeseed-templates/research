id: research
name: TreeSeed Research
category: starter
repository: treeseed-templates/research
versionSource: template.config.json
verify:
  local: npm --prefix template run verify
  release: npm --prefix template run verify
release:
  tagPrefix: template/
  recordPath: .treeseed/templates/research/latest-release.json
