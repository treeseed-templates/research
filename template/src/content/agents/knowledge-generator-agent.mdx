---
name: Knowledge Generator Agent
slug: knowledge-generator-agent
handler: report
projectAgentClassId: knowledge
projectAgentClassSlug: knowledge
handlerConfig:
  workPackageKind: report
  domain: knowledge_draft
enabled: true
description: Converts research output into durable knowledge entries.
summary: Shapes research into publishable content.
operator: research-steward
runtimeStatus: active
capabilities: [synthesis, drafting]
tags: [agent, knowledge]
systemPrompt: |
  You are the Knowledge Generator Agent. Turn research notes into structured knowledge that can be reused in books and packs.
persona: Structured and editorial.
triggers:
  - type: message
    messageTypes: [research_completed]
permissions:
  - model: knowledge
    operations: [create, update]
  - model: message
    operations: [create, pick, update]
contentAccess:
  read:
    models: [question, objective, note, book, knowledge, person]
    actions: [describe, query, read]
    books: ["*"]
  write:
    models: [question, note, knowledge]
    actions: [create, update, link, validate]
    books: ["*"]
    relations: [note.related_questions, note.related_objectives, question.related_objectives]
  commit:
    allowed: false
context:
  queries:
    - id: knowledge-generator-agent-treedx-context
      purpose: generate
      query: research note source map knowledge draft optimization uncertainty packaging readiness
      scope: /
      codeScopes:
        - src/content
        - docs
      relations:
        - references
        - related
      depth: 2
      budget: 12
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: read_only
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - knowledge_drafting
      - source_map
    fallbackPolicy: require_same_model_class
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: knowledge-generator
  requiresApprovedWork: true
  contextSource: treedx
  contextFormat: mdx
  contextInjection: rendered-file-content
  audit:
    capture: [prompt, context, response, toolLog, changedFiles, capacityEnvelope, treeDxVerification]
    redact: [secrets, credentials]
tools:
  allowed:
    - treedx.build_context
    - treedx.read_repository_files
    - treedx.search_workspace
    - treedx.read_workspace_file
    - treeseed.status
    - treeseed.content.query
    - treeseed.content.read
    - treeseed.questions.create
    - treeseed.notes.create
    - treeseed.knowledge.create
    - treeseed.content.link_note
outputs:
  messageTypes: [knowledge_generated]
  modelMutations: [knowledge:create, message:create]
governance:
  mutationClass: draft_or_optimization
---

The generator turns gathered evidence into structured project knowledge.
